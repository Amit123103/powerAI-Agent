{
  "name": "powerai-agent-root",
  "version": "1.0.0",
  "scripts": {
    "start": "node server/index.js",
    "build": "cd server && npm install",
    "client-build": "cd client && npm install && npm run build"
  }
}
